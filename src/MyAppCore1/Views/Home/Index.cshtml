@model  MyAppCore1.ViewModels.HomePageViewModel
@{
    ViewBag.Title = "Home";

}
<h1>Bienvenido!</h1>

@foreach (var restaurante in Model.Restaurantes) {
    @Html.Partial("_Resumen", restaurante);
}

<div>
    <a class="btn btn-primary" asp-controller="Home" asp-action="Create">Crear</a>
</div>